Tower run evidence — honest, exact CLI output (2026-05-31 00:13 UTC).

================================================================
RESULT: The full LOST.guide pipeline RUNS GREEN + LIVE through Tower's
runtime via `tower run --local`. Verified with the literal log below.
Key is supplied by a TOWER SECRET (not the shell env), Nimble runs LIVE.
================================================================

== HOW (reproducible) ==
1. Run from a LOCAL drive (NOT the B:\ UNC share — that caused every earlier SpawnFailed).
2. Towerfile app name = "lostguide-local" — a NON-deployed name. (A deployed app name makes
   `tower run --local` fail: "Running apps by name locally is not supported yet".)
3. requirements.txt INCLUDES `tower` — so the `tower` SDK is in the --local venv and
   `tower.secret("NIMBLE_API_KEY")` can fetch the key. (Without it, import tower fails and the
   run falls back to stub mode.)
4. NIMBLE_API_KEY is stored as a Tower secret (`tower secrets create --name=NIMBLE_API_KEY ...`).
   It does NOT need to be in the shell env — Tower injects it via the secret store.
5. Command from the app dir: `tower run --local`

== EXACT stdout (key source proven; shell env was cleared before launch) ==
  Success! Launching app `lostguide-local`
  2026-05-31 00:13:00 | Using CPython 3.13.6
  2026-05-31 00:13:00 | Creating virtual environment at: .venv
  2026-05-31 00:13:01 | Resolved 30 packages in 280ms
  2026-05-31 00:13:09 | Prepared 27 packages in 8.04s
  2026-05-31 00:13:11 | Installed 27 packages in 1.55s
  2026-05-31 00:13:11 | [pipeline] tower_sdk=True key_source=tower-secret key_present=True
  2026-05-31 00:13:19 | knockout complete
  2026-05-31 00:13:19 | wrote knockout_runs + knockout_conflicts
  2026-05-31 00:13:19 | { "mark":"BANKSY AI", "stub": false, "nimble_calls": 6,
                         "n_conflicts": 27, "risk_score":"HIGH",
                         "recommendation":"DO_NOT_PROCEED", "sink":"local-parquet" }
  Success! Your local run exited cleanly.

=> Tower provisioned a venv, installed deps (incl. the tower SDK), pulled NIMBLE_API_KEY from the
   Tower secret store, ran the REAL agent with 6 LIVE Nimble calls (stub=false), produced 27
   risk-scored conflicts (HIGH / DO_NOT_PROCEED), wrote both lakehouse-schema'd tables, exited 0.

== Sink note ==
sink=local-parquet because no Iceberg catalog is configured on the account (`tower catalogs list`
is empty). Enabling Tower's managed Iceberg catalog would switch the sink to persistent
knockout_runs / knockout_conflicts tables — same code path (pipeline.py already calls
tower.tables(...).create_if_not_exists(schema).insert(...)).

== Still NOT working: Tower CLOUD run ==
`tower run` (remote): exited 0, errored 11; pods die in ~1s at setup, exit_code null. Reproduced
with a zero-dependency print() app. Not quota (beta plan, 1000 compute minutes, seconds used).
Server-side pod-spawn issue — only Tower can fix; see TOWER_SUPPORT_EMAIL.md. The --local path
above is Tower's documented, recommended development path and is fully working.
