Tower run evidence — honest, exact CLI output (2026-05-31).

================================================================
SUMMARY
- `tower run --local` EXECUTES the full pipeline through Tower's runtime
  (venv + deps + run + clean exit) — reproduced 5x. ✅
- It runs in STUB mode because Tower's --local runtime injects NO secrets on
  this CLI version (v0.3.63): a dedicated probe proved tower.secret() and the
  env both return empty for BOTH account secrets. So LIVE Nimble inside a Tower
  run needs the cloud runtime — which is separately broken server-side.
- Plain `python pipeline.py` IS fully live (6 Nimble calls, ~27 conflicts). That
  is the verified live path for the submission.
================================================================

== WHAT WORKS: tower run --local executes the pipeline ==
From a LOCAL drive, Towerfile app name = "lostguide-local" (NON-deployed):
  Success! Launching app `lostguide-local`
  Using CPython 3.13.6 | Creating virtual environment | Installed deps
  { "mark":"BANKSY AI", "stub": true, "nimble_calls": 0, "n_conflicts": 7,
    "risk_score":"HIGH", "recommendation":"DO_NOT_PROCEED", "sink":"local-parquet" }
  Success! Your local run exited cleanly.
Diagnostic line from pipeline.py: [pipeline] tower_sdk=True key_source=none key_present=False
=> Tower's local runtime works on this machine. (Earlier SpawnFailed = the B:\ UNC drive;
   a DEPLOYED app name makes --local refuse: "Running apps by name locally is not supported yet".)

== WHY STILL STUB: secret-injection probe (definitive) ==
A throwaway app `secret-probe` (just prints what the runtime sees), `tower run --local`,
shell env cleared. Exact stdout:
  === SECRET PROBE ===
  env NIMBLE_API_KEY present: False
  env TOWER vars: []
  tower.secret('NIMBLE_API_KEY') -> len=0 present=False
  tower.secret('hack')          -> len=0 present=False
  is_cloud_run: False
  === END PROBE ===
=> On CLI v0.3.63, `tower run --local` does NOT inject secrets (neither as env vars nor via
   tower.secret()), for EITHER secret. Our --local runs also never print the docs'
   "Getting secrets... Done!" line, consistent with secret-fetch not happening in local mode.
   Tower's docs say secrets ARE injected as env vars on --local, so this looks like a
   version/behavior gap. Live Nimble therefore requires the CLOUD runtime.

== CLOUD runtime: still blocked server-side ==
`tower run` (remote): exited 0, errored 11; pods die in ~1s at setup, exit_code null.
Reproduced with a zero-dependency print() app. Not quota (beta plan, 1000 compute min, seconds
used). Server-side pod-spawn issue — only Tower can fix. See TOWER_SUPPORT_EMAIL.md.

== Net, honest ==
- Pipeline correctness: VERIFIED LIVE via plain `python pipeline.py` (6 calls, ~27 conflicts).
- Tower deploy: works. Tower --local: executes the pipeline (stub, due to no secret injection).
- Tower live run (local or cloud): blocked by Tower (no --local secrets; cloud pod-spawn fails).
